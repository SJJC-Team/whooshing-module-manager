---
testing1:
    domain: "testing1"
    pgsql:
        "default":
            database: "testing"
            port: 0
        "pg2":
            database: "testing2"
            port: 10
    api:
        "default":
            port: 1
            pgDatabasePorts: [0, 10]
            domain: "api"
            bundle: "Path to bundle"
    inline:
        "default":
            port: 2
            pgDatabasePorts: [0, 10]
            bundle: "Path to bundle"
    https:
        "test":
            port: 3
            pgDatabasePorts: [0]
            domain: "web"
            bundle: "Path to bundle"
